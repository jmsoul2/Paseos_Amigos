# Entorno y secretos
.env
.env.*
!.env.example

# Directorio active/ — scratch y memoria de trabajo (no es código de la app)
active/

# Handoff de diseño (referencia local, no va al repo)
*.zip

# Node
node_modules/
dist/
build/

# Netlify (build local / netlify dev)
.netlify/

# OS y editor
.DS_Store
Thumbs.db
.vscode/
*.swp
*~

# Claude Code (config local de la máquina, no se comparte)
.claude/settings.local.json
